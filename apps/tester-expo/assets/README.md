# Fixture assets

These files are copied from existing Re.Pack repository fixtures so the Expo
tester remains self-contained:

- `images/repack.png` comes from `apps/tester-app/src/miniapp/callstack-dark.png`;
- `images/density*.png` come from `apps/tester-app/src/assetsTest/localAssets`;
- `fonts/material-symbols-regular.ttf` comes from the installed
  `@expo-google-fonts/material-symbols` package and is distributed under the
  Apache License 2.0 in `fonts/LICENSE-MATERIAL-SYMBOLS`.
